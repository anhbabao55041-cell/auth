{
  "name": "cyberauth-service",
  "version": "1.0.0",
  "description": "CyberAuth Security Identity & License Control Service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "pip3 install -r requirements.txt || pip install -r requirements.txt || echo 'python ready'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "CyberAuth",
  "license": "ISC"
}
